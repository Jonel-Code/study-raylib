# Studying Raylib

a repository build for learing raylib and basic c++

### How to build and run:
open terminal and run on project directory:

1. `mkdir build`
2. `cd ./build` 
3. `cmake ..`
4. `cmake --build .` this will create a Debug folder which contains Clicker.exe which is the entrypoint of the application